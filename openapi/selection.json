{
  "$comment": "The OpenAPI operations (`METHOD /path`) whose models `pnpm gen:api` emits; `\"*\"` = the full spec. Managed by `pnpm gen:api:pick`; hand-edits are fine — gen:api validates every key against the spec.",
  "operations": [
    "GET /v1/notifications",
    "GET /v1/notifications/unread-count",
    "GET /v1/users/me",
    "GET /v1/users/referral/{code}",
    "GET /v1/users/referrals",
    "POST /v1/auth/refresh",
    "POST /v1/auth/signin",
    "POST /v1/auth/signup",
    "PUT /v1/notifications/{notificationId}"
  ]
}
